# The three services together, for real — no stub model, no sink. The compose
# at the repo root is the self-contained demo; this file is the family with
# its own credentials. Run from the repo root:
#
#   docker compose --env-file .env \
#     -f deploy/docker-compose.yml up -d --build
#
# `--env-file .env` matters: compose interpolates ${...} from the compose
# file's own directory by default, and the ${...:?} guards below must read
# the deployment root's .env instead.
#
# What .env must carry, and why:
#   HOOKRELAY_CONFIG_FILE=...   your pipe config — see hookrelay/examples/
#                               stack.yaml for the shape, but write real
#                               channels: its demo ones post to containers
#                               this file deliberately does not run. Use an
#                               ABSOLUTE path (e.g. $PWD/config.yaml):
#                               compose resolves a relative bind source
#                               against this file's directory, not your cwd
#   HOOKPROBE_TOKEN=...         the investigator refuses to start without auth
#   HOOKJUDGE_AI_*              optional — empty means every event walks the
#                               free routes down to the rule floor
#   ANTHROPIC_API_KEY=...       or the Anthropic-dialect trio + model aliases
#
# Ports are asymmetric on purpose: only the pipe faces the outside world;
# the judge's and the investigator's admin pages stay on loopback for ssh
# tunnels. Inside the network everyone reaches everyone by DNS name.

name: hookstack-family

services:
  hookrelay:
    build: ../hookrelay
    image: hookrelay:latest
    container_name: hookrelay
    environment:
      HOOKRELAY_HOST: 0.0.0.0
      HOOKRELAY_PORT: 8100
      HOOKRELAY_DB: /data/hookrelay.db
      HOOKRELAY_CONFIG: /etc/hookrelay/config.yaml
      HOOKRELAY_WORKER_INTERVAL: "0.5"
      HOOKRELAY_PLUGINS: /data/plugins
      # Where an escalation route delivers, if your config references it.
      HOOKPROBE_EVENT_URL: ${HOOKPROBE_EVENT_URL:-http://hookprobe:8088/hooks/event}
    volumes:
      - ${HOOKRELAY_CONFIG_FILE:?path to your config.yaml — sources, routes, channels}:/etc/hookrelay/config.yaml:ro
      - relay-data:/data
    # The whole .env is injected, so a ${NAME} in the config only needs a
    # line in .env — no compose edit, no forgotten passthrough.
    env_file:
      - path: ../.env
        required: false
    ports:
      - "8100:8100"
    restart: unless-stopped

  hookjudge:
    build: ../hookjudge
    image: hookjudge:latest
    container_name: hookjudge
    environment:
      HOOKJUDGE_HOST: 0.0.0.0
      HOOKJUDGE_PORT: 8200
      HOOKJUDGE_DB: /data/hookjudge.db
      # One outbound address, and it is the pipe. Fan-out is not its business.
      HOOKJUDGE_RETURN_URL: http://hookrelay:8100/hook/judge-notify
      HOOKJUDGE_WORKER_INTERVAL: "0.5"
      HOOKJUDGE_REUSE_WINDOW_SECONDS: ${HOOKJUDGE_REUSE_WINDOW_SECONDS:-3600}
      HOOKJUDGE_AI_BASE_URL: ${HOOKJUDGE_AI_BASE_URL:-}
      HOOKJUDGE_AI_API_KEY: ${HOOKJUDGE_AI_API_KEY:-}
      HOOKJUDGE_AI_MODEL: ${HOOKJUDGE_AI_MODEL:-}
      HOOKJUDGE_AI_PRICE_IN_PER_1K: ${HOOKJUDGE_AI_PRICE_IN_PER_1K:-0.00015}
      HOOKJUDGE_AI_PRICE_OUT_PER_1K: ${HOOKJUDGE_AI_PRICE_OUT_PER_1K:-0.0006}
      # Direct self-alarm when a return goes dead — the pipe is the broken
      # link at that moment, so this posts straight to a bot/collector URL.
      HOOKJUDGE_ALARM_URL: ${HOOKJUDGE_ALARM_URL:-}
      HOOKJUDGE_ALARM_MIN_INTERVAL_SECONDS: ${HOOKJUDGE_ALARM_MIN_INTERVAL_SECONDS:-}
    volumes:
      - judge-data:/data
    ports:
      - "127.0.0.1:8200:8200"
    restart: unless-stopped

  hookprobe:
    build: ../hookprobe
    image: hookprobe:latest
    container_name: hookprobe
    environment:
      HOOKPROBE_TOKEN: ${HOOKPROBE_TOKEN:?set HOOKPROBE_TOKEN in .env}
      HOOKPROBE_MODEL: ${HOOKPROBE_MODEL:-claude-opus-5}
      HOOKPROBE_RETURN_URL: http://hookrelay:8100/hook/probe-notify
      HOOKPROBE_ESCALATE_LEVELS: ${HOOKPROBE_ESCALATE_LEVELS:-critical,high}
      # Window spend >= budget refuses new escalations (report card says why).
      HOOKPROBE_BUDGET_USD: ${HOOKPROBE_BUDGET_USD:-}
      HOOKPROBE_BUDGET_WINDOW_HOURS: ${HOOKPROBE_BUDGET_WINDOW_HOURS:-}
      # Case files and transcripts older than this are pruned daily. 0 keeps
      # everything — the case files are the agent's episodic memory.
      HOOKPROBE_RETENTION_DAYS: ${HOOKPROBE_RETENTION_DAYS:-}
      # Skill layers: "project" (distilled, on the volume) is always on;
      # add "user" to also load a host library mounted at
      # /data/home/.claude/skills. HOOKPROBE_SKILLS pins the session's
      # skill list (comma names or "all") so a big library cannot flood it.
      HOOKPROBE_SETTING_SOURCES: ${HOOKPROBE_SETTING_SOURCES:-}
      HOOKPROBE_SKILLS: ${HOOKPROBE_SKILLS:-}
      # Operator methodology appended to the system prompt (a file path;
      # default: {workdir}/system-prompt.md when present). Named subagent
      # roles come from a JSON file (name -> {description, prompt, ...}).
      HOOKPROBE_SYSTEM_PROMPT_APPEND: ${HOOKPROBE_SYSTEM_PROMPT_APPEND:-}
      HOOKPROBE_AGENTS_CONFIG: ${HOOKPROBE_AGENTS_CONFIG:-}
      # Loop hygiene, all advisory (the boundary stays the bash guard):
      # nudge the agent after N identical calls, and cap how long any single
      # command may hang. 0 = off.
      HOOKPROBE_REPEAT_REMINDER_AT: ${HOOKPROBE_REPEAT_REMINDER_AT:-}
      HOOKPROBE_BASH_TIMEOUT_MS: ${HOOKPROBE_BASH_TIMEOUT_MS:-}
      HOOKPROBE_BASH_MAX_TIMEOUT_MS: ${HOOKPROBE_BASH_MAX_TIMEOUT_MS:-}
      # Model-call telemetry. On by default and harmless when nothing is
      # listening — measured: with no endpoint a run finishes in the same time,
      # emits no retries and logs nothing. So the switch stays on and the only
      # decision left is where to send it: set OTEL_EXPORTER_OTLP_ENDPOINT when
      # you have a collector, and the data starts arriving with no restart of
      # anything else. What arrives per LLM call: claude_code.api_request
      # {model, input_tokens, output_tokens, cache_read_tokens,
      # cache_creation_tokens, cost_usd, duration_ms, session.id, prompt.id,
      # event.sequence}, plus assistant_response, tool_result, tool_decision,
      # hook_execution_*, mcp_server_connection, user_prompt, and the counters
      # token.usage / cost.usage / session.count / active_time.total.
      # NOTE the cost_usd it reports prices Claude models; on another provider
      # re-price from the raw token counts and the real model name.
      CLAUDE_CODE_ENABLE_TELEMETRY: ${CLAUDE_CODE_ENABLE_TELEMETRY:-1}
      OTEL_LOGS_EXPORTER: ${OTEL_LOGS_EXPORTER:-otlp}
      OTEL_METRICS_EXPORTER: ${OTEL_METRICS_EXPORTER:-otlp}
      OTEL_EXPORTER_OTLP_PROTOCOL: ${OTEL_EXPORTER_OTLP_PROTOCOL:-http/json}
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-}
      OTEL_EXPORTER_OTLP_HEADERS: ${OTEL_EXPORTER_OTLP_HEADERS:-}
      OTEL_RESOURCE_ATTRIBUTES: ${OTEL_RESOURCE_ATTRIBUTES:-}
      OTEL_METRIC_EXPORT_INTERVAL: ${OTEL_METRIC_EXPORT_INTERVAL:-}
      # Content redaction is the CLI's own (<REDACTED>), not this repository's,
      # and it is five independent switches. Measured on 2.1.229: responses=1
      # exposes the answers and leaves prompts redacted, while prompts=1 exposes
      # both. So the answers are on by default — they are what a report is judged
      # on, and their inputs are already kept upstream in the pipe's ledger —
      # while the inbound side, which carries the alert payload and whatever the
      # environment memory says, stays off until someone decides otherwise.
      OTEL_LOG_ASSISTANT_RESPONSES: ${OTEL_LOG_ASSISTANT_RESPONSES:-1}
      OTEL_LOG_USER_PROMPTS: ${OTEL_LOG_USER_PROMPTS:-}
      # Heavier still, and off: tool arguments, whole tool outputs (the megabyte
      # `grep` the harness already truncates), and the raw request/response
      # bodies of every model call.
      OTEL_LOG_TOOL_DETAILS: ${OTEL_LOG_TOOL_DETAILS:-}
      OTEL_LOG_TOOL_CONTENT: ${OTEL_LOG_TOOL_CONTENT:-}
      OTEL_LOG_RAW_API_BODIES: ${OTEL_LOG_RAW_API_BODIES:-}
      # Direct self-alarm when report returns exhaust their retries:
      HOOKPROBE_ALARM_URL: ${HOOKPROBE_ALARM_URL:-}
      HOOKPROBE_ALARM_MIN_INTERVAL_SECONDS: ${HOOKPROBE_ALARM_MIN_INTERVAL_SECONDS:-}
      # Wire signatures for both directions of the family loop. Empty means
      # unsigned doors — fine on this file's own private network, wrong on a
      # shared one. Each is one line of .env; set both when in doubt.
      HOOKPROBE_EVENT_SECRET: ${HOOKPROBE_EVENT_SECRET:-}
      HOOKPROBE_RETURN_SECRET: ${HOOKPROBE_RETURN_SECRET:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      ANTHROPIC_BASE_URL: ${ANTHROPIC_BASE_URL:-}
      ANTHROPIC_AUTH_TOKEN: ${ANTHROPIC_AUTH_TOKEN:-}
      # Any Anthropic-dialect provider: map the engine's model aliases here.
      ANTHROPIC_MODEL: ${ANTHROPIC_MODEL:-}
      ANTHROPIC_DEFAULT_HAIKU_MODEL: ${ANTHROPIC_DEFAULT_HAIKU_MODEL:-}
      ANTHROPIC_DEFAULT_SONNET_MODEL: ${ANTHROPIC_DEFAULT_SONNET_MODEL:-}
      ANTHROPIC_DEFAULT_OPUS_MODEL: ${ANTHROPIC_DEFAULT_OPUS_MODEL:-}
      CLAUDE_CODE_SUBAGENT_MODEL: ${CLAUDE_CODE_SUBAGENT_MODEL:-}
      API_TIMEOUT_MS: ${API_TIMEOUT_MS:-}
      # MCP servers config (three dialects accepted; env values are
      # secrets — keep the file mounted ro or on the volume):
      HOOKPROBE_MCP_CONFIG: ${HOOKPROBE_MCP_CONFIG:-}
    volumes:
      - probe-data:/data
      # A host skills library as the read-only user layer — mount ONLY the
      # skills subdir, never the whole ~/.claude (credentials live there):
      # - ~/.claude/skills:/data/home/.claude/skills:ro
      # Read-only credentials are the real security boundary — mount only
      # query-grade identities:
      # - ../secrets/kubeconfig-readonly:/data/home/.kube/config:ro
    ports:
      - "127.0.0.1:8088:8088"
    restart: unless-stopped
    depends_on:
      - hookrelay

volumes:
  relay-data:
  judge-data:
  probe-data:

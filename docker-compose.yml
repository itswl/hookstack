services:
  hookjudge:
    build: .
    container_name: hookjudge
    ports:
      - "127.0.0.1:8200:8200"
    volumes:
      - ./data:/data
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
